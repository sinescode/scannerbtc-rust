# Security Policy

## Threat Model

### Assumptions

1. **Trusted inputs**: The scanner operates on user-provided files (TSV, Bloom filter).
   These files are assumed to be from trusted sources or validated before use.

2. **Untrusted derived data**: Keys, addresses, and mnemonics generated by the scanner
   are ephemeral. The scanner does not store long-lived secrets.

3. **No network access**: The scanner operates entirely offline. It does not connect
   to any network services.

4. **Single-user operation**: The scanner is designed for single-user desktop use.
   It does not implement multi-user access control.

### Attacker Model

An attacker may:

1. **Provide malformed input files**: Corrupted TSV, Bloom filter, or other data files.
   The scanner must handle these gracefully without crashes or undefined behavior.

2. **Observe memory**: In some scenarios, an attacker may have access to process memory
   (e.g., memory dumps, cold boot attacks). Sensitive material should be minimized.

3. **Observe timing**: The scanner should not leak information through timing side channels
   in its cryptographic operations.

### Failure Modes

1. **Invalid Bloom filter**: Returns `BloomError` with descriptive message. Never silently
   produces incorrect results.

2. **Invalid mnemonic**: Returns `MnemonicError`. Never accepts invalid mnemonics.

3. **Invalid private key**: Returns `None` or `DerivationError`. Never produces invalid keys.

4. **File I/O errors**: Returns `std::io::Error` or `BloomError::Io`. Never panics.

### Security Properties

1. **No secret persistence**: Private keys, mnemonics, and seeds are not written to disk
   unless explicitly requested by the user (via `--output`).

2. **Memory zeroization**: Sensitive key material in `XKey` is zeroed on drop.

3. **Constant-time operations**: Cryptographic operations use library implementations
   that are designed to be constant-time where applicable.

4. **No unsafe code in crypto**: All cryptographic operations use safe Rust wrappers
   around well-tested libraries.

### Out of Scope

- Network security (scanner is offline)
- Multi-user access control
- Hardware security modules (HSM)
- Secure key storage (user responsibility)
- Key recovery mechanisms

## Reporting Vulnerabilities

If you discover a security vulnerability, please report it responsibly by opening
a GitHub issue with the "security" label. Do not disclose vulnerabilities publicly
until a fix is available.
